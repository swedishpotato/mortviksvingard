# Mortviks Vingard

## Running locally

This project uses Ruby 3.3.6 (pinned via `.tool-versions` for mise). If you have mise installed, the correct Ruby version will be selected automatically.

```bash
bundle install
bundle exec jekyll serve
```

The site will be available at `http://localhost:4000`.
